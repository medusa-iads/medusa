global:
  scrape_interval: 3s
  evaluation_interval: 3s

rule_files:
  - "/etc/prometheus/rules.yml"

scrape_configs:
  - job_name: "medusa"
    static_configs:
      - targets: ["node-exporter:9100"]
